# Packages to be installed in `Dockerfile` in `train` or `full` modes.
# PyTorch and CUDA related libraries should be installed here.
# Packages unavailable in `conda` should also be placed here.
pytorch-lightning==1.5.9
torchmetrics==0.7.0
hydra-core==1.1.0
hydra_colorlog==1.1.0
jupyterlab==3.2.8
seaborn==0.11.2
